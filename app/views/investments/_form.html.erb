<%= form_for(investment) do |f| %>
  <% if investment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(investment.errors.count, "error") %> prohibited this investment from being saved:</h2>

      <ul>
      <% investment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :scheme_id %>
   
    <%= f.collection_select :scheme_id, Scheme.all, :id, :name %>
   
  </div>

  <div class="field">
    <%=f.label :customer_id %>
  
     <%=f.hidden_field :customer_id, :value => current_customer.id %>
  <%= current_customer.name %>
  </div>

  <div class="field">
    <%= f.label :date %>
    <%= f.date_select :date %>
  </div>

  <div class="field">
    <%= f.label :amount %>
    <%= f.number_field :amount %>
        
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
